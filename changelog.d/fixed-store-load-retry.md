- Your app data recovers on its own when a file can't be read: the next access
  retries instead of leaving settings, local issues and pull requests, review
  history, or automations unavailable until you restart — and an edit made in
  the meantime is refused rather than saved over what's on disk.
