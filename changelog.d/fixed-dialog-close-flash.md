- Confirm dialogs hold their contents while closing instead of flashing blank
  or stale copy for the last instant — the repository-files and merge-picker
  confirms, the stash-drop prompt, the task-run prompt, and the MCP replace
  prompt all keep their title, description, and button labels through the
  close animation.
