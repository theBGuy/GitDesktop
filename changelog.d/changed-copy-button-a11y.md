- Icon-only copy buttons (webhook URLs and payloads, `gh` commands, review
  threads, sign-in links, window coordinates, AI connection errors) now tell
  screen readers what they copy and show the app's focus ring when you tab to
  them.
