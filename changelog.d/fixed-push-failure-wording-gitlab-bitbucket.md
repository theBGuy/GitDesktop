- Push failures explain themselves on GitLab and Bitbucket too: a push to an
  archived GitLab project reads as such, and a Bitbucket remote with no stored
  password no longer falls back to raw Git text.
