- While a GitHub or GitLab API rate limit is in effect, the command palette's
  **Reconnect forge session** steps aside (a fresh sign-in draws on the same
  quota); it returns once GitDesktop sees the limit clear.
