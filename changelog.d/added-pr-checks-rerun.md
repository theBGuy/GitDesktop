- **Re-run failed CI from a pull request.** One click on the checks rollup retries
  the failed jobs (**Re-run failed jobs** on GitHub) or the pipeline (**Retry
  pipeline** on GitLab), with no trip to the Actions tab. The command palette
  carries it too, as **Re-run failed checks**.
